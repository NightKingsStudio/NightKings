# Enable mod_rewrite (required for redirect rules)
RewriteEngine On

# Redirect root URL to /home
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ /home [L,R=301]

# Custom 404 page
ErrorDocument 404 /404.html
